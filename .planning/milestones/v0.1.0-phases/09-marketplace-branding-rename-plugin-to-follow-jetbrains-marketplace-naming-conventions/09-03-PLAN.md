---
phase: 09-marketplace-branding-rename-plugin-to-follow-jetbrains-marketplace-naming-conventions
plan: 03
type: execute
wave: 2
depends_on:
  - "09-01"
  - "09-02"
files_modified:
  - README.md
  - CHANGELOG.md
autonomous: false

must_haves:
  truths:
    - "README.md plugin description section opens with 'Mako adds language support' (not 'Mako Template Support')"
    - "README.md description mentions <%def>, <%block>, ${...}, <%inherit>, % for, % if"
    - "README.md description covers all 8 features: syntax highlighting, folding, structure view, brace matching, comment toggling, Python injection, tag completion, error annotations"
    - "CHANGELOG.md title is '# Mako Changelog'"
    - "CHANGELOG.md has a [0.1.0] section documenting the rename"
    - "gradlew clean check passes with exit code 0"
    - "No com.github.kimuth references remain anywhere in the repository tracked files"
  artifacts:
    - path: "README.md"
      provides: "Marketplace plugin description (extracted by Gradle between marker comments)"
      contains: "Mako adds language support"
    - path: "CHANGELOG.md"
      provides: "Release notes for Marketplace and changelog plugin"
      contains: "[0.1.0]"
  key_links:
    - from: "README.md"
      to: "build.gradle.kts markdownToHTML()"
      via: "<!-- Plugin description --> markers"
      pattern: "<!-- Plugin description -->"
    - from: "CHANGELOG.md"
      to: "build.gradle.kts changeNotes ="
      via: "changelog.getOrNull(pluginVersion)"
      pattern: "## \\[0\\.1\\.0\\]"
---

<objective>
Update Marketplace-facing content (README.md description, CHANGELOG.md), then run a full clean build to verify the entire rename compiles and all tests pass.

Purpose: README.md is the source of the Marketplace description (extracted by Gradle between marker comments). CHANGELOG.md provides release notes. Both must reflect the new name before submission. The final build verification confirms the complete rename is coherent — all three source trees, grammar files, and build config work together.
Output: README.md with updated description, CHANGELOG.md with 0.1.0 entry and updated title, confirmed green build (./gradlew clean check passes).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-marketplace-branding-rename-plugin-to-follow-jetbrains-marketplace-naming-conventions/09-RESEARCH.md
@.planning/phases/09-marketplace-branding-rename-plugin-to-follow-jetbrains-marketplace-naming-conventions/09-01-SUMMARY.md
@.planning/phases/09-marketplace-branding-rename-plugin-to-follow-jetbrains-marketplace-naming-conventions/09-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update README.md and CHANGELOG.md</name>
  <files>
    README.md
    CHANGELOG.md
  </files>
  <action>
    Edit README.md — replace ONLY the content between the plugin description markers. The markers themselves must remain on their own lines exactly as written. Replace everything between them (inclusive of the blank lines around the content) with:

    <!-- Plugin description -->
    **Mako** adds language support for [Mako](https://www.makotemplates.org/) templates in PyCharm and other JetBrains IDEs.

    ### Features

    - **Syntax highlighting** — distinct colors for `<%def>`, `<%block>`, `<%inherit>`, `${...}` expressions, control lines (`% for`, `% if`), and comments (`##`, `<%doc>`)
    - **Code folding** — collapse `<%def>`, `<%block>`, and control flow blocks; `<%doc>` and `<%!>` sections fold by default on file open
    - **Structure view** — navigate all `<%def>` and `<%block>` declarations in the Structure panel (Ctrl+F12)
    - **Brace matching** — `${` and `}` are highlighted as matched pairs
    - **Comment toggling** — `##` line comments via Ctrl+/ and `<%doc>` block comments via Ctrl+Shift+/
    - **Python injection** — Python syntax highlighting and analysis inside `${...}`, `<% %>`, and `<%! %>` regions
    - **Tag completion** — autocomplete for Mako directive names after `<%` and attribute names inside open tags
    - **Error annotations** — red squiggles for unclosed `<%def>` and `<%block>` tags

    Requires PyCharm Community or Professional 2025.2+.
    <!-- Plugin description end -->

    Also update the README.md title line (line 1) from "# jetbrains-mako-template-plugin" to "# Mako" and update the Installation section search term from "Search for \"jetbrains-mako-template-plugin\"" to "Search for \"Mako\"".

    Edit CHANGELOG.md:
    1. Change the title line from "# Mako Template Support Changelog" to "# Mako Changelog"
    2. Add the 0.1.0 entry between [Unreleased] and [0.0.1]. The final structure should be:

    # Mako Changelog

    ## [Unreleased]

    ## [0.1.0]
    ### Changed
    - Renamed plugin to **Mako** (was: Mako Template Support) following JetBrains Marketplace naming guidelines
    - Plugin ID changed to `com.schtilig.mako` (was: `com.github.kimuth.jetbrainsmakotemplateplugin`)
    - Vendor updated to **Schtilig**
    - Source packages renamed to `com.schtilig.mako`

    ## [0.0.1]
    ### Added
    ...
    (keep existing 0.0.1 content unchanged)
  </action>
  <verify>
    grep "Mako Template Support" README.md → 0 matches
    grep "Mako adds language support" README.md → 1 match (in description section)
    grep "<%def>" README.md → at least 1 match (in features list)
    grep "# Mako Changelog" CHANGELOG.md → 1 match (line 1)
    grep "\[0.1.0\]" CHANGELOG.md → 1 match
    grep "com.schtilig.mako" CHANGELOG.md → 1 match (in 0.1.0 entry)
  </verify>
  <done>
    README.md: description section opens with "**Mako** adds language support", mentions all required Mako constructs and all 8 features, ends with requirements note. Title updated to "# Mako".
    CHANGELOG.md: title is "# Mako Changelog", [0.1.0] entry present with rename/rebrand notes, [0.0.1] entry unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run full clean build to verify rename</name>
  <files>build/</files>
  <action>
    Run the full build with clean to bypass Gradle cache (cached stale class files from old package paths would cause ClassNotFoundException at runtime):
      ./gradlew clean check

    This runs compilation + all tests. Expected outcome: BUILD SUCCESSFUL.

    If the build fails, diagnose the failure:
    - "package com.github.kimuth does not exist" → a file was missed in the package rename; find it with: grep -r "com.github.kimuth" src/
    - "ClassNotFoundException" or "NoClassDefFoundError" → Gradle cache issue; confirm `clean` was included
    - "Plugin description section not found" → README.md marker comments are malformed; verify exact text of the markers
    - "Unresolved reference" in Kotlin → an import was not updated; grep for the unresolved class name across src/

    Fix any issues found, then re-run ./gradlew clean check until it passes.

    After a successful build, do a final sweep to confirm no old package references remain in any tracked file:
      git grep "com.github.kimuth" → should return 0 results
  </action>
  <verify>
    ./gradlew clean check exits with BUILD SUCCESSFUL
    git grep "com.github.kimuth" → 0 matches
  </verify>
  <done>
    ./gradlew clean check passes with BUILD SUCCESSFUL. All tests pass. Zero com.github.kimuth references remain in any git-tracked file. The plugin is fully renamed to com.schtilig.mako with display name "Mako" and version 0.1.0.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify complete rename and build</name>
  <what-built>
    Full package rename from com.github.kimuth.jetbrainsmakotemplateplugin to com.schtilig.mako across all source layers (grammar files, Gradle config, Kotlin main sources, generated Java, test sources, plugin.xml, README.md, CHANGELOG.md). Plugin display name is now "Mako", ID is "com.schtilig.mako", version is 0.1.0.
  </what-built>
  <how-to-verify>
    1. Confirm the build passed: check that Task 2 above completed with BUILD SUCCESSFUL (no further action needed if already confirmed).
    2. Open src/main/resources/META-INF/plugin.xml and verify:
       - id: com.schtilig.mako
       - name: Mako
       - vendor: Schtilig with url="https://github.com/Kimuth/jetbrains-mako-template-plugin"
    3. Open gradle.properties and verify:
       - pluginGroup = com.schtilig.mako
       - pluginName = Mako
       - pluginVersion = 0.1.0
    4. Open README.md and confirm the description section starts with "**Mako** adds language support".
    5. Open CHANGELOG.md and confirm the title is "# Mako Changelog" and a [0.1.0] entry exists.
    6. Optional: run ./gradlew runIde and open a .mako file to confirm the plugin loads correctly with the new identity (Settings > Plugins should show "Mako" not "Mako Template Support").
  </how-to-verify>
  <resume-signal>Type "approved" to mark Phase 9 complete, or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
Final phase verification:
  git grep "com.github.kimuth" → 0 results
  git grep "Mako Template Support" → 0 results
  ./gradlew clean check → BUILD SUCCESSFUL
  grep "pluginVersion" gradle.properties → "pluginVersion = 0.1.0"
  grep "\[0.1.0\]" CHANGELOG.md → 1 match
</verification>

<success_criteria>
Phase 9 complete when:
- README.md description section updated with new name and all 8 features
- CHANGELOG.md title and 0.1.0 entry correct
- ./gradlew clean check passes (all tests green, BUILD SUCCESSFUL)
- git grep "com.github.kimuth" returns 0 results
- Human checkpoint approved
</success_criteria>

<output>
After completion, create `.planning/phases/09-marketplace-branding-rename-plugin-to-follow-jetbrains-marketplace-naming-conventions/09-03-SUMMARY.md`
</output>
