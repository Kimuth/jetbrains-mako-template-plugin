---
phase: 17-clean-up-orphaned-test-fixtures
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/test/testData/rename/foo.xml
  - src/test/testData/rename/foo_after.xml
  - src/test/testData/folding/FoldingTestData.mako
  - src/test/testData/annotator/InvalidDirective.mako
  - src/test/testData/annotator/UnclosedBlockTag.mako
  - src/test/testData/annotator/UnclosedDefTag.mako
autonomous: true
requirements: []

must_haves:
  truths:
    - "Every file in src/test/testData/ is referenced by at least one test method"
    - "No test fixture exists for a test that has been migrated to inline content"
    - "./gradlew check passes with zero errors after deletions"
  artifacts:
    - path: "src/test/testData/annotator/WellFormedDefTag.mako"
      provides: "Annotator negative test fixture (MUST be retained — used by testWellFormedDefTagNoError)"
    - path: "src/test/testData/parser/"
      provides: "Parser fixture pairs (all 7 pairs must remain — fully exercised by MakoParsingTest)"
  key_links:
    - from: "MakoAnnotatorTest.testWellFormedDefTagNoError"
      to: "src/test/testData/annotator/WellFormedDefTag.mako"
      via: "myFixture.configureByFile(\"annotator/WellFormedDefTag.mako\")"
      pattern: "configureByFile.*WellFormedDefTag"
---

<objective>
Delete five orphaned test fixture files that are no longer referenced by any test method.

Purpose: The test suite has evolved to use inline content (configureMakoFile / parseFile with string literals) for annotator, folding, and PSI tests. Five disk fixture files and two XML scaffold remnants from the initial project template now exist in testData/ with no corresponding test method loading them. Leaving them creates confusion about what is tested and what the test expectations are.

Output: Five files deleted, ./gradlew check still passes, testData/ contains only fixtures actively loaded by a test method.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete orphaned rename/ scaffold fixtures</name>
  <files>
    src/test/testData/rename/foo.xml
    src/test/testData/rename/foo_after.xml
  </files>
  <action>
    Delete the two XML files from the rename/ directory. These are IntelliJ plugin project template scaffold files committed in the Initial commit. No test class in the project references "rename/", "foo.xml", or "foo_after.xml" anywhere — confirmed by full-codebase grep. They are unrelated to Mako language support.

    After deletion, if the rename/ directory is empty, also remove the empty directory from git tracking:
      git rm src/test/testData/rename/foo.xml src/test/testData/rename/foo_after.xml

    Do NOT delete src/test/testData/rename/ itself as a separate step — git rm will handle the directory removal implicitly when its last tracked file is removed.
  </action>
  <verify>
    git ls-files src/test/testData/rename/ | wc -l
    # Expected: 0 (no tracked files remain)
    ls src/test/testData/rename/ 2>/dev/null || echo "directory gone"
  </verify>
  <done>
    foo.xml and foo_after.xml are absent from the working tree and from git index. No test failure is introduced (these files were never loaded by any test).
  </done>
</task>

<task type="auto">
  <name>Task 2: Delete orphaned annotator and folding fixture files</name>
  <files>
    src/test/testData/annotator/InvalidDirective.mako
    src/test/testData/annotator/UnclosedBlockTag.mako
    src/test/testData/annotator/UnclosedDefTag.mako
    src/test/testData/folding/FoldingTestData.mako
  </files>
  <action>
    Delete four fixture files that are no longer loaded from disk by any test method.

    Evidence for each deletion:
    - annotator/InvalidDirective.mako: MakoAnnotatorTest.testInvalidDirectiveShowsError uses configureMakoFile() with inline string content ("<%bogus attr=\"x\">\ncontent\n"). The file on disk is never loaded.
    - annotator/UnclosedBlockTag.mako: MakoAnnotatorTest.testUnclosedBlockTagShowsError uses configureMakoFile() with inline string content. The file on disk is never loaded.
    - annotator/UnclosedDefTag.mako: MakoAnnotatorTest.testUnclosedDefTagShowsError uses configureMakoFile() with inline string content. The file on disk is never loaded.
    - folding/FoldingTestData.mako: MakoFoldingTest uses ParsingTestCase("folding", ...) as the test data subdirectory but ALL test methods call buildFolds() with the SAMPLE_MAKO inline string or construct inline content directly. configureByFile() is never called in MakoFoldingTest. FoldingTestData.mako is never referenced by name.

    IMPORTANT: Do NOT delete annotator/WellFormedDefTag.mako — it IS used by MakoAnnotatorTest.testWellFormedDefTagNoError via myFixture.configureByFile("annotator/WellFormedDefTag.mako").

    Run:
      git rm src/test/testData/annotator/InvalidDirective.mako \
             src/test/testData/annotator/UnclosedBlockTag.mako \
             src/test/testData/annotator/UnclosedDefTag.mako \
             src/test/testData/folding/FoldingTestData.mako

    After the git rm, if the folding/ directory is now empty, it will be removed implicitly.
    The annotator/ directory will remain (WellFormedDefTag.mako stays).
  </action>
  <verify>
    git ls-files src/test/testData/annotator/
    # Expected: only WellFormedDefTag.mako remains

    git ls-files src/test/testData/folding/
    # Expected: empty (no tracked files)

    # Verify WellFormedDefTag.mako is still present
    ls src/test/testData/annotator/WellFormedDefTag.mako

    # Run the full test suite to confirm no regressions
    ./gradlew check
  </verify>
  <done>
    The four files are deleted. annotator/WellFormedDefTag.mako still exists. ./gradlew check passes with zero failures. The only files remaining in testData/ are either loaded by a test method or are the parser/ fixtures (which are all exercised by MakoParsingTest).
  </done>
</task>

</tasks>

<verification>
After both tasks complete, verify the testData/ directory is clean:

  git ls-files src/test/testData/
  # Expected output contains ONLY:
  #   src/test/testData/annotator/WellFormedDefTag.mako
  #   src/test/testData/parser/BlockTagFollowedByText.mako
  #   src/test/testData/parser/BlockTagFollowedByText.txt
  #   src/test/testData/parser/ConsecutiveExpressions.mako
  #   src/test/testData/parser/ConsecutiveExpressions.txt
  #   src/test/testData/parser/DefTagFollowedByText.mako
  #   src/test/testData/parser/DefTagFollowedByText.txt
  #   src/test/testData/parser/ExpressionFollowedByText.mako
  #   src/test/testData/parser/ExpressionFollowedByText.txt
  #   src/test/testData/parser/MalformedTag.mako
  #   src/test/testData/parser/MalformedTag.txt
  #   src/test/testData/parser/UnknownDirective.mako
  #   src/test/testData/parser/UnknownDirective.txt
  #   src/test/testData/parser/WellFormedFile.mako
  #   src/test/testData/parser/WellFormedFile.txt

  ./gradlew check
  # Expected: BUILD SUCCESSFUL
</verification>

<success_criteria>
- rename/foo.xml and rename/foo_after.xml deleted from git tracking
- annotator/InvalidDirective.mako, annotator/UnclosedBlockTag.mako, annotator/UnclosedDefTag.mako deleted
- folding/FoldingTestData.mako deleted
- annotator/WellFormedDefTag.mako retained (still loaded by testWellFormedDefTagNoError)
- All parser/ fixture pairs retained (all 7 pairs exercised by MakoParsingTest)
- ./gradlew check passes: BUILD SUCCESSFUL with zero test failures
- git ls-files src/test/testData/ shows exactly the 15 files listed in verification above
</success_criteria>

<output>
After completion, create `.planning/phases/17-clean-up-orphaned-test-fixtures/17-01-SUMMARY.md`
</output>
