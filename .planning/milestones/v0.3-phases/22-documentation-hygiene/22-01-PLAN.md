---
phase: 22-documentation-hygiene
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "REQUIREMENTS.md HINJ-01 entry no longer says 'FAILED verification (18-02)' — says 'PASS (Phase 20)'"
    - "REQUIREMENTS.md HINJ-04 entry no longer says 'PARTIAL verification (18-03)' — says 'PASS (Phase 20)'"
    - "REQUIREMENTS.md HINJ-05 entry says 'PASS (Phase 21)' and traceability says 'Complete'"
    - "REQUIREMENTS.md HINJ-06 entry says 'PASS (Phase 21)' and traceability says 'Complete'"
    - "ROADMAP.md 19-01-PLAN.md checkbox is [x]"
    - "ROADMAP.md 20-01, 20-02, 20-03 plan checkboxes are [x]"
    - "ROADMAP.md 21-01, 21-02 plan checkboxes are [x]"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Accurate requirement statuses and traceability for HINJ-01/04/05/06"
      contains: "PASS (Phase 20)"
    - path: ".planning/ROADMAP.md"
      provides: "Correct plan-completion checkboxes for phases 19–21"
      contains: "[x] 19-01-PLAN.md"
  key_links:
    - from: "REQUIREMENTS.md requirement-list"
      to: "REQUIREMENTS.md traceability table"
      via: "Both locations must be updated for HINJ-01/04/05/06"
      pattern: "Both the `- [x] \\*\\*HINJ-XX\\*\\*:` line and the `| HINJ-XX |` row"
---

<objective>
Apply all documentation corrections to bring REQUIREMENTS.md and ROADMAP.md into alignment with actual Phase 20 and Phase 21 verification outcomes.

Purpose: Phases 20 and 21 verified HINJ-01, HINJ-04, HINJ-05, and HINJ-06 as PASS, but planning docs still say "FAILED", "PARTIAL", or "Pending". Plan checklists for completed phases 19–21 still show unchecked boxes. This is the final cleanup needed to close v0.3.0 with accurate records.

Output: Updated REQUIREMENTS.md (9 targeted line replacements) and ROADMAP.md (checkbox corrections + Phase 22 plans field).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/22-documentation-hygiene/22-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply all REQUIREMENTS.md and ROADMAP.md documentation corrections</name>
  <files>
    .planning/REQUIREMENTS.md
    .planning/ROADMAP.md
  </files>
  <action>
Read both files before writing (required by Write tool).

Then apply ALL of the following edits in a single write pass per file.

--- REQUIREMENTS.md EDITS ---

Edit 1 — HINJ-01 requirement-list entry (line ~12):
REPLACE:
  - [x] **HINJ-01**: User sees HTML syntax coloring in template body regions of `.mako` files — FAILED verification (18-02); HTML tags not colored differently from Mako constructs
WITH:
  - [x] **HINJ-01**: User sees HTML syntax coloring in template body regions of `.mako` files — PASS (Phase 20); MakoEditorHighlighter confirmed working at runtime; HTML tags visually colored in TEMPLATE_TEXT regions

Edit 2 — HINJ-04 requirement-list entry (line ~15):
REPLACE:
  - [x] **HINJ-04**: User sees HTML error squiggles for malformed markup in template body — PARTIAL verification (18-03): squiggles appear on `<span>` without closing tag but NOT on `<p>` or `<html>` without closing tags; inconsistent by element type; carry to Phase 20
WITH:
  - [x] **HINJ-04**: User sees HTML error squiggles for malformed markup in template body — PASS (Phase 20); behavior confirmed correct per HTML5 spec: `<span>` without close gets squiggle (required-close element); `<p>` without close does not (optional-close element)

Edit 3 — HINJ-05 requirement-list entry (line ~16):
REPLACE:
  - [x] **HINJ-05**: CSS completion and validation are active inside `<style>` tags in `.mako` files — FAILED verification (18-02/20-03); MultiHostInjector/LanguageInjectionContributor for CSSLanguage not registered; gap closure Phase 21
WITH:
  - [x] **HINJ-05**: CSS completion and validation are active inside `<style>` tags in `.mako` files — PASS (Phase 21); MakoCssInjector implemented and registered; CSS completions confirmed active in running IDE when CSS plugin installed

Edit 4 — HINJ-06 requirement-list entry (line ~17):
REPLACE:
  - [x] **HINJ-06**: JavaScript completion and validation are active inside `<script>` tags in `.mako` files — FAILED verification (18-02/20-03); MultiHostInjector/LanguageInjectionContributor for JavaScriptLanguage not registered; gap closure Phase 21
WITH:
  - [x] **HINJ-06**: JavaScript completion and validation are active inside `<script>` tags in `.mako` files — PASS (Phase 21); platform HtmlScriptLanguageInjector handles `<script>` XmlText automatically; JavaScript completions confirmed active in running IDE when JavaScript plugin installed

Edit 5 — Traceability table HINJ-01 row (line ~69):
REPLACE:
  | HINJ-01 | Phase 18/20 | Verification failed — HTML coloring gap; carry to Phase 20 |
WITH:
  | HINJ-01 | Phase 18/20 | Complete — Phase 20 PASS; MakoEditorHighlighter confirmed working at runtime 2026-02-22 |

Edit 6 — Traceability table HINJ-04 row (line ~72):
REPLACE:
  | HINJ-04 | Phase 18/20 | PARTIAL verification (18-03) — squiggles on span tags but not p or html tags; inconsistent coverage; carry to Phase 20 |
WITH:
  | HINJ-04 | Phase 18/20 | Complete — Phase 20 PASS; behavior correct per HTML5 spec (optional-close elements don't require squiggle) verified 2026-02-22 |

Edit 7 — Traceability table HINJ-05 row (line ~73):
REPLACE:
  | HINJ-05 | Phase 18/20/21 | Pending — gap closure Phase 21 (LanguageInjectionContributor for CSSLanguage not registered) |
WITH:
  | HINJ-05 | Phase 18/20/21 | Complete — Phase 21 PASS; MakoCssInjector registered; CSS completions verified in running IDE 2026-02-22 |

Edit 8 — Traceability table HINJ-06 row (line ~74):
REPLACE:
  | HINJ-06 | Phase 18/20/21 | Pending — gap closure Phase 21 (LanguageInjectionContributor for JavaScriptLanguage not registered) |
WITH:
  | HINJ-06 | Phase 18/20/21 | Complete — Phase 21 PASS; platform HtmlScriptLanguageInjector handles JS; completions verified in running IDE 2026-02-22 |

Edit 9 — Last-updated footer (last line of file, line ~88):
REPLACE:
  *Last updated: 2026-02-22 after v0.3.0 milestone audit — HINJ-05/06 reset to Pending; assigned to Phase 21 gap closure*
WITH:
  *Last updated: 2026-02-23 after Phase 22 documentation hygiene — HINJ-01/04/05/06 traceability updated to reflect Phase 20/21 PASS outcomes*

--- ROADMAP.md EDITS ---

Before writing ROADMAP.md, read it and verify the current checkbox state of each line. The 18-03-PLAN.md line may already show [x] — only change it if it shows [ ].

Edit 10 — 18-03-PLAN.md checkbox: Verify current state. If [ ], change to [x]. If already [x], no change.

Edit 11 — 19-01-PLAN.md checkbox (line ~83):
REPLACE:
  - [ ] 19-01-PLAN.md — Apply MakoStructureViewFactory dual-tree guard, add regression test, run ./gradlew check, verify IDE success criteria
WITH:
  - [x] 19-01-PLAN.md — Apply MakoStructureViewFactory dual-tree guard, add regression test, run ./gradlew check, verify IDE success criteria

Edit 12 — 20-01/02/03-PLAN.md checkboxes (lines ~97-99):
REPLACE all three [ ] with [x]:
  - [x] 20-01-PLAN.md — Implement MakoEditorHighlighter, MakoEditorHighlighterProvider, MakoErrorFilter + plugin.xml registration + CRCT automated tests
  - [x] 20-02-PLAN.md — Run ./gradlew check; verify all tests pass including two new CRCT tests
  - [x] 20-03-PLAN.md — Human IDE verification of CRCT-01/02 false-positive suppression and HINJ-01/04/05/06 feature behavior

Edit 13 — 21-01/02-PLAN.md checkboxes (lines ~113-114):
REPLACE both [ ] with [x]:
  - [x] 21-01-PLAN.md — Implement MakoCssInjector (MultiHostInjector for XmlText in &lt;style&gt;), register in plugin.xml, add MakoCssInjectorTest, run ./gradlew check
  - [x] 21-02-PLAN.md — Human IDE verification of CSS completions (HINJ-05) and JavaScript completions (HINJ-06) in running IDE

Edit 14 — Phase 22 Plans field (line ~126):
REPLACE:
  **Plans**: TBD
WITH:
  **Plans**: 1 plan
  Plans:
  - [ ] 22-01-PLAN.md — Apply all REQUIREMENTS.md and ROADMAP.md documentation corrections
  </action>
  <verify>
Run these grep checks after writing both files:

grep "HINJ-01" .planning/REQUIREMENTS.md
  — must NOT contain "FAILED" or "carry to Phase 20"
  — must contain "PASS (Phase 20)"

grep "HINJ-04" .planning/REQUIREMENTS.md
  — must NOT contain "PARTIAL" or "carry to Phase 20"
  — must contain "PASS (Phase 20)"

grep "HINJ-05" .planning/REQUIREMENTS.md
  — requirement-list entry must contain "PASS (Phase 21)"
  — traceability row must contain "Complete"

grep "HINJ-06" .planning/REQUIREMENTS.md
  — same pattern as HINJ-05

grep "19-01-PLAN" .planning/ROADMAP.md
  — must show [x]

grep -E "20-0[123]-PLAN|21-0[12]-PLAN" .planning/ROADMAP.md
  — all must show [x]
  </verify>
  <done>
All 9 REQUIREMENTS.md edits applied: HINJ-01/04/05/06 requirement-list entries show PASS outcomes; traceability table rows show Complete; footer shows 2026-02-23 date. All ROADMAP.md checkboxes for completed plans in phases 19-21 show [x]. Phase 22 Plans field updated from TBD to 1 plan.
  </done>
</task>

</tasks>

<verification>
Re-read both files after writing and confirm:
1. No "FAILED verification" or "carry to Phase 20" text remains on HINJ-01 or HINJ-04 lines
2. No "PARTIAL" text remains on HINJ-04 line
3. No "Pending" text remains on HINJ-05 or HINJ-06 traceability rows
4. grep "19-01-PLAN" .planning/ROADMAP.md shows [x]
5. grep -E "20-0[123]-PLAN|21-0[12]-PLAN" .planning/ROADMAP.md shows [x] for all
</verification>

<success_criteria>
1. REQUIREMENTS.md HINJ-01 entry reflects Phase 20 PASS — MakoEditorHighlighter confirmed working
2. REQUIREMENTS.md HINJ-04 entry reflects Phase 20 PASS — HTML5 spec-correct behavior
3. REQUIREMENTS.md HINJ-05 and HINJ-06 entries reflect Phase 21 PASS — completions verified
4. REQUIREMENTS.md traceability table rows for HINJ-01/04/05/06 all say "Complete"
5. ROADMAP.md 19-01-PLAN.md checkbox shows [x]
6. ROADMAP.md 20-01/02/03 and 21-01/02 plan checkboxes all show [x]
7. REQUIREMENTS.md last-updated footer reflects 2026-02-23
</success_criteria>

<output>
After completion, create `.planning/phases/22-documentation-hygiene/22-01-SUMMARY.md`
</output>
