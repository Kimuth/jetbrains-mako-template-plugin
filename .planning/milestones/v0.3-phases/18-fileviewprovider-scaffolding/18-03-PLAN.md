---
phase: 18-fileviewprovider-scaffolding
plan: "03"
type: execute
wave: 3
depends_on:
  - "18-02"
files_modified: []
autonomous: false
gap_closure: true
requirements:
  - HINJ-01
  - HINJ-04
  - HINJ-05
  - HINJ-06

must_haves:
  truths:
    - "HINJ-04: Human has confirmed whether HTML error squiggles appear on malformed HTML in a .mako file in the running IDE"
    - "HINJ-01: Decision to defer HTML syntax coloring to Phase 20 is explicitly recorded with rationale"
    - "HINJ-05: Decision to defer CSS sub-injection in style tags to Phase 20 is explicitly recorded with rationale"
    - "HINJ-06: Decision to defer JS sub-injection in script tags to Phase 20 is explicitly recorded with rationale"
  artifacts:
    - path: ".planning/phases/18-fileviewprovider-scaffolding/18-03-SUMMARY.md"
      provides: "Gap closure summary recording HINJ-04 verification result and HINJ-01/05/06 deferral rationale"
  key_links:
    - from: "HINJ-04 checkpoint result"
      to: "REQUIREMENTS.md"
      via: "HINJ-04 status update in Traceability table"
      pattern: "HINJ-04.*verified|HINJ-04.*deferred"
---

<objective>
Close the remaining Phase 18 verification gaps: verify HINJ-04 (HTML error squiggles) in the running IDE, and explicitly defer HINJ-01 (HTML syntax coloring), HINJ-05 (CSS sub-injection), and HINJ-06 (JS sub-injection) to Phase 20 with recorded rationale.

Purpose: The 18-02 human verification session confirmed HINJ-02 and HINJ-03 working. HINJ-04 was not tested in that session. HINJ-01, HINJ-05, and HINJ-06 failed verification but require architectural work (TemplateLanguageSyntaxHighlighter, MultiHostInjector) that belongs in Phase 20's "HTML Feature Verification and False-Positive Audit" scope. This plan closes Phase 18 cleanly by verifying the one remaining runtime behavior and documenting the deferrals.

Output: Human confirmation of HINJ-04 status; explicit deferral records for HINJ-01/05/06; Phase 18 declared complete.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-fileviewprovider-scaffolding/18-02-SUMMARY.md
</context>

<deferral_rationale>
## Why HINJ-01, HINJ-05, HINJ-06 are deferred to Phase 20 (not re-planned here)

**HINJ-01 — HTML syntax coloring not active in TEMPLATE_TEXT regions**

Root cause (confirmed by 18-02 verification): TemplateLanguageFileViewProvider creates the HTML PSI tree but the platform's syntax highlighter dispatch does not automatically consult the HTML SyntaxHighlighter for TEMPLATE_TEXT tokens. MakoSyntaxHighlighter handles TEMPLATE_TEXT with a plain attribute and no delegation.

Fix required: Implement TemplateLanguageSyntaxHighlighter (wraps MakoSyntaxHighlighter and delegates TEMPLATE_TEXT ranges to HTMLSyntaxHighlighter) or register a SyntaxHighlighterFactory that redirects. This is non-trivial and interacts with color scheme mapping — scoped to Phase 20.

REQUIREMENTS.md traceability: "HINJ-01 | Phase 18/20 | Verification failed — HTML coloring gap; carry to Phase 20"

**HINJ-05 — CSS sub-injection inside style tags not working**

Root cause (confirmed by 18-02 verification): The HTML Emmet handler fires across the entire HTML PSI tree including style content; the platform's HTML-to-CSS injection does not automatically activate within the HTML PSI tree created by TemplateLanguageFileViewProvider. Explicit LanguageInjectionContributor or MultiHostInjector wiring is required.

REQUIREMENTS.md traceability: "HINJ-05 | Phase 18/20 | Verification failed — CSS sub-injection gap; carry to Phase 20"

**HINJ-06 — JS sub-injection inside script tags not working**

Root cause: Same as HINJ-05 — HTML Emmet fires instead of JS-specific handlers; HtmlScriptContentProvider does not automatically activate within the TemplateLanguageFileViewProvider-managed HTML tree in this configuration.

REQUIREMENTS.md traceability: "HINJ-06 | Phase 18/20 | Verification failed — JS sub-injection gap; carry to Phase 20"

All three deferrals are recorded in STATE.md (decisions log, Known Gaps section) and REQUIREMENTS.md (Traceability table). Phase 20 ("HTML Feature Verification and False-Positive Audit") explicitly covers CSS completion (success criterion #4) and JavaScript completion (success criterion #5).
</deferral_rationale>

<tasks>

<task type="auto">
  <name>Task 1: Launch test IDE for HINJ-04 verification</name>
  <files>
    <!-- No tracked source files modified. Launches the IDE for human verification. -->
  </files>
  <action>
Launch the test IDE with the plugin loaded:

```bash
./gradlew runIde
```

Wait for the IDE window to fully open. Once open, create a new .mako file named `hinj04_test.mako` containing deliberately malformed HTML:

```
<%def name="broken()">
<div>
    <p>This paragraph is never closed
    <span>Nested without proper close
</div>
</%def>

## Template body with malformed HTML
<html>
<body>
    <div>
        <p>Unclosed paragraph
    </div>
</body>
```

Open this file in the editor. The IDE is ready for HINJ-04 human verification.
  </action>
  <verify>IDE window is open and the .mako file with malformed HTML is displayed in the editor.</verify>
  <done>IDE running with the HINJ-04 fixture file open showing malformed HTML markup.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — HINJ-04 HTML error squiggles</name>
  <files>
    <!-- No tracked source files modified by this checkpoint. -->
  </files>
  <action>
Human verifies HINJ-04 (HTML error squiggles) in the running IDE using the fixture file opened in Task 1.

MakoFileViewProvider (wired in 18-01) creates a parallel HTML PSI tree for every .mako file. The HTML PSI tree enables the platform's HTML annotators to run on TEMPLATE_TEXT regions. HINJ-04 expects that malformed HTML tags produce red error squiggles — the same behavior as in a plain .html file. No code changes are made in this plan: the structural precondition (dual PSI tree) was verified in 18-01 automated tests.

With the hinj04_test.mako file open in the running IDE, check the following:

1. HTML error squiggles (HINJ-04): Look at the unclosed paragraph and unclosed span tags in the TEMPLATE_TEXT region (the lines outside the <%def> block). Do red squiggles appear on the malformed or unclosed tags? Compare with behavior in a plain .html file — the same HTML errors should produce squiggles in both.

2. No false-positive squiggles on Mako constructs: The <%def name="broken()">, </%def>, ## Template body lines should NOT have red HTML error squiggles. These are Mako syntax — the HTML annotator should treat them as opaque outer language elements.

Report one of the following outcomes:
- "HINJ-04 PASS" — red squiggles appear on malformed HTML, no false positives on Mako constructs
- "HINJ-04 FAIL: no squiggles" — malformed HTML produces no red squiggles at all
- "HINJ-04 FAIL: false positives on [lines]" — red squiggles appear on Mako constructs
- "HINJ-04 PARTIAL: [description]" — squiggles appear but with caveats
  </action>
  <verify>Human has reported the HINJ-04 verification outcome (PASS, FAIL, or PARTIAL).</verify>
  <done>Human has typed one of the four outcome strings: "HINJ-04 PASS", "HINJ-04 FAIL: no squiggles", "HINJ-04 FAIL: false positives on [lines]", or "HINJ-04 PARTIAL: [description]".</done>
  <what-built>
    MakoFileViewProvider (18-01) creates a parallel HTML PSI tree for every .mako file, enabling HTML annotators in TEMPLATE_TEXT regions. HINJ-04 was not tested in the 18-02 session — this checkpoint closes that gap.
  </what-built>
  <how-to-verify>
Open hinj04_test.mako in the running IDE. Check for red squiggles on the unclosed paragraph and span tags. Verify no false positives appear on Mako control lines (<%def>, </%def>, ## comment lines).
  </how-to-verify>
  <resume-signal>Type the HINJ-04 outcome: "HINJ-04 PASS", "HINJ-04 FAIL: no squiggles", "HINJ-04 FAIL: false positives on [lines]", or "HINJ-04 PARTIAL: [description]".</resume-signal>
</task>

</tasks>

<verification>
Gap closure is complete when:

1. Human has reported HINJ-04 verification result (PASS, FAIL, or PARTIAL)
2. HINJ-01, HINJ-05, HINJ-06 deferrals are documented in the SUMMARY with rationale linking to Phase 20
3. REQUIREMENTS.md HINJ-04 status is updated to reflect the verification result
4. STATE.md Known Gaps section is updated: remove HINJ-04 if PASS, keep if FAIL/PARTIAL with new note
</verification>

<success_criteria>
- Human has tested HINJ-04 in the running IDE and reported the result
- If HINJ-04 PASS: REQUIREMENTS.md updated to mark HINJ-04 satisfied; STATE.md Known Gaps removes HINJ-04 entry
- If HINJ-04 FAIL/PARTIAL: REQUIREMENTS.md HINJ-04 status updated; new gap note added to STATE.md; carry-forward to Phase 20 noted
- HINJ-01, HINJ-05, HINJ-06 deferrals explicitly documented in 18-03-SUMMARY.md with Phase 20 forward reference
- Phase 18 is declared complete (all plans executed, gaps either closed or explicitly deferred with recorded rationale)
</success_criteria>

<output>
After completion, create `.planning/phases/18-fileviewprovider-scaffolding/18-03-SUMMARY.md` using the summary template.

The summary MUST include:
- HINJ-04 verification result (verbatim from human)
- HINJ-01 deferral rationale (root cause: no automatic syntax highlighter delegation from TemplateLanguageFileViewProvider; fix: TemplateLanguageSyntaxHighlighter in Phase 20)
- HINJ-05 deferral rationale (root cause: HTML Emmet fires across entire HTML tree; fix: LanguageInjectionContributor or MultiHostInjector in Phase 20)
- HINJ-06 deferral rationale (same as HINJ-05)
- Phase 18 completion declaration

Also update:
- `.planning/REQUIREMENTS.md` — HINJ-04 status in the HINJ table and Traceability table based on verification result
- `.planning/STATE.md` — remove or update HINJ-04 from Known Gaps; add Phase 18 completion note
</output>
