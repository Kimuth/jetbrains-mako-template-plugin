---
phase: 04-syntax-highlighting-comment-support
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/kotlin/com/github/kimuth/jetbrainsmakotemplateplugin/lang/highlighting/MakoSyntaxHighlighter.kt
autonomous: true
requirements:
  - SYNX-02
gap_closure: true

must_haves:
  truths:
    - "Expression constructs (${...}) display in a distinct color from surrounding template text"
  artifacts:
    - path: "src/main/kotlin/com/github/kimuth/jetbrainsmakotemplateplugin/lang/highlighting/MakoSyntaxHighlighter.kt"
      provides: "MAKO_EXPRESSION TextAttributesKey with visually distinct fallback color"
      contains: "MARKUP_TAG"
  key_links:
    - from: "MakoSyntaxHighlighter.MAKO_EXPRESSION"
      to: "DefaultLanguageHighlighterColors.MARKUP_TAG"
      via: "createTextAttributesKey fallback parameter"
      pattern: "createTextAttributesKey.*MAKO_EXPRESSION.*MARKUP_TAG"
---

<objective>
Fix expression highlighting so ${...} expressions display in a visually distinct color instead of blending with plain text.

Purpose: UAT test 1 failed because MAKO_EXPRESSION falls back to TEMPLATE_LANGUAGE_COLOR, which inherits from HighlighterColors.TEXT (plain text). Expressions are invisible to the user. Changing the fallback to MARKUP_TAG gives expressions a distinct color in all standard color schemes (typically dark cyan/teal in Darcula, dark blue in Light).
Output: Updated MakoSyntaxHighlighter.kt with MAKO_EXPRESSION using MARKUP_TAG fallback.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-syntax-highlighting-comment-support/04-01-SUMMARY.md
@src/main/kotlin/com/github/kimuth/jetbrainsmakotemplateplugin/lang/highlighting/MakoSyntaxHighlighter.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Change MAKO_EXPRESSION fallback from TEMPLATE_LANGUAGE_COLOR to MARKUP_TAG</name>
  <files>src/main/kotlin/com/github/kimuth/jetbrainsmakotemplateplugin/lang/highlighting/MakoSyntaxHighlighter.kt</files>
  <action>
In MakoSyntaxHighlighter.kt, change line 21-22 from:

```kotlin
val MAKO_EXPRESSION = createTextAttributesKey(
    "MAKO_EXPRESSION", DefaultLanguageHighlighterColors.TEMPLATE_LANGUAGE_COLOR
)
```

to:

```kotlin
val MAKO_EXPRESSION = createTextAttributesKey(
    "MAKO_EXPRESSION", DefaultLanguageHighlighterColors.MARKUP_TAG
)
```

Rationale: TEMPLATE_LANGUAGE_COLOR inherits from HighlighterColors.TEXT which has no visible foreground color, making expressions indistinguishable from plain text. MARKUP_TAG provides a distinct color in all standard color schemes:
- Darcula: dark cyan/teal
- Light/IntelliJ: dark blue/navy
- High Contrast: visible foreground

This is a single-line change. Do NOT change any other TextAttributesKey fallback. Do NOT change any other code in the file.

After the edit, run `./gradlew build` to confirm the build passes with all 22 tests green.
  </action>
  <verify>
1. `./gradlew build` passes with BUILD SUCCESSFUL and all 22 tests green
2. Grep the file to confirm the change: the string "MARKUP_TAG" appears on the MAKO_EXPRESSION line and "TEMPLATE_LANGUAGE_COLOR" does NOT appear anywhere in the file
  </verify>
  <done>
MAKO_EXPRESSION TextAttributesKey uses DefaultLanguageHighlighterColors.MARKUP_TAG as its fallback color, ensuring ${...} expressions are visually distinct from surrounding template text in all standard color schemes.
  </done>
</task>

</tasks>

<verification>
1. `./gradlew build` passes — no regressions from the single-line change
2. MakoSyntaxHighlighter.kt contains `DefaultLanguageHighlighterColors.MARKUP_TAG` for MAKO_EXPRESSION
3. MakoSyntaxHighlighter.kt does NOT contain `TEMPLATE_LANGUAGE_COLOR` anywhere
4. All other TextAttributesKey constants remain unchanged
</verification>

<success_criteria>
- Build passes with all 22 tests green
- MAKO_EXPRESSION fallback is MARKUP_TAG (not TEMPLATE_LANGUAGE_COLOR)
- No other changes to the highlighter
</success_criteria>

<output>
After completion, create `.planning/phases/04-syntax-highlighting-comment-support/04-02-SUMMARY.md`
</output>
