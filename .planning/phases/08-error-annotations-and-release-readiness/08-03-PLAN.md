---
phase: 08-error-annotations-and-release-readiness
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - CHANGELOG.md
  - src/main/resources/META-INF/pluginIcon.svg
autonomous: false
requirements: [COMP-03]

must_haves:
  truths:
    - "README.md plugin description section contains real Mako Template Support content (not placeholder text)"
    - "CHANGELOG.md has a [0.0.1] section listing implemented features"
    - "META-INF/pluginIcon.svg exists as a 40x40px SVG (Marketplace listing icon)"
    - "./gradlew verifyPlugin completes without binary compatibility errors"
  artifacts:
    - path: "README.md"
      provides: "Plugin description between <!-- Plugin description --> markers"
      contains: "Mako Template Support"
    - path: "CHANGELOG.md"
      provides: "Release notes for version 0.0.1"
      contains: "## [0.0.1]"
    - path: "src/main/resources/META-INF/pluginIcon.svg"
      provides: "40x40px SVG for Marketplace plugin listing"
      contains: "<svg"
  key_links:
    - from: "build.gradle.kts"
      to: "README.md"
      via: "providers.fileContents(layout.projectDirectory.file(\"README.md\")).asText"
      pattern: "Plugin description"
    - from: "build.gradle.kts"
      to: "CHANGELOG.md"
      via: "changelog { } + changeNotes provider"
      pattern: "getOrNull.*pluginVersion"
---

<objective>
Update the plugin's Marketplace-facing content — README description, CHANGELOG, and plugin icon — and confirm ./gradlew verifyPlugin passes. These are the release-readiness gates before Marketplace submission.

Purpose: The build.gradle.kts extracts plugin description from README.md and change notes from CHANGELOG.md. Both currently contain scaffold placeholder text, which would result in a rejected or unprofessional Marketplace listing. The pluginIcon.svg is required for the listing page icon.

Output: README.md with real description, CHANGELOG.md with 0.0.1 release notes, META-INF/pluginIcon.svg, verified build.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-error-annotations-and-release-readiness/08-RESEARCH.md
@README.md
@CHANGELOG.md
@build.gradle.kts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update README description, CHANGELOG, and create pluginIcon.svg</name>
  <files>
    README.md
    CHANGELOG.md
    src/main/resources/META-INF/pluginIcon.svg
  </files>
  <action>
**1. README.md — replace plugin description section:**

Replace the content between `<!-- Plugin description -->` and `<!-- Plugin description end -->` markers (keep the markers themselves — build.gradle.kts requires them). Replace with:

```markdown
<!-- Plugin description -->
**Mako Template Support** provides first-class language support for [Mako](https://www.makotemplates.org/) templates in PyCharm and other JetBrains IDEs.

### Features

- **Syntax highlighting** — distinct colors for Mako directives (`<%def>`, `<%block>`, `<%inherit>`, etc.), expressions (`${...}`), control lines (`% for`, `% if`), and comments (`##`, `<%doc>`)
- **Code folding** — collapse `<%def>`, `<%block>`, and control flow blocks; `<%doc>` and `<%!>` fold by default on file open
- **Structure view** — navigate all `<%def>` and `<%block>` declarations in the Structure panel (Ctrl+F12)
- **Brace matching** — `${` and `}` are highlighted as matched pairs
- **Comment toggling** — `##` line comments via Ctrl+/ and `<%doc>` block comments via Ctrl+Shift+/
- **Python injection** — Python syntax highlighting and analysis inside `${...}`, `<% %>`, and `<%! %>` regions
- **Tag completion** — autocomplete for Mako directive names after `<%` and attribute names inside open tags
- **Error annotations** — red squiggles for unclosed `<%def>` and `<%block>` tags

### Requirements

- PyCharm Community or Professional 2025.2+
<!-- Plugin description end -->
```

Keep all other README content (badges section at top, installation section, template footer) as-is. Only replace content between the markers.

**2. CHANGELOG.md — add real release entry:**

Replace the entire file content with:

```markdown
<!-- Keep a Changelog guide -> https://keepachangelog.com -->

# Mako Template Support Changelog

## [Unreleased]

## [0.0.1]
### Added
- Syntax highlighting for Mako directives, expressions, control lines, and comments with customizable color scheme
- Code folding for `<%def>`, `<%block>`, control flow blocks (`% for`, `% if`, `% while`); `<%doc>` and `<%!>` fold by default
- Structure view panel showing all `<%def>` and `<%block>` declarations as a navigable tree
- Brace matching between `${` and `}` delimiters
- Line comment toggling (`##`) via Ctrl+/ and block comment toggling (`<%doc>`) via Ctrl+Shift+/
- Python language injection into expression (`${...}`), code block (`<% %>`), and module block (`<%! %>`) regions
- Autocomplete for Mako directive names after `<%` and attribute names inside open tags
- Error annotations for unclosed `<%def>` and `<%block>` tags
```

**3. src/main/resources/META-INF/pluginIcon.svg — create Marketplace plugin icon:**

Create a minimal 40x40px SVG. The icon should be simple and recognizable. Use the letter "M" in a rounded square, matching the Mako file icon style (dark teal/green background used for the file type icon):

```svg
<svg xmlns="http://www.w3.org/2000/svg" width="40" height="40" viewBox="0 0 40 40">
  <rect width="40" height="40" rx="8" ry="8" fill="#2B6B6B"/>
  <text x="20" y="28" font-family="JetBrains Mono, monospace, sans-serif" font-size="22" font-weight="bold" fill="#FFFFFF" text-anchor="middle">M</text>
</svg>
```

Place at: `src/main/resources/META-INF/pluginIcon.svg`

Note: The Marketplace also accepts `pluginIcon_dark.svg` for dark themes, but it is optional. The light version is sufficient.
  </action>
  <verify>./gradlew buildPlugin 2>&1 | tail -20</verify>
  <done>./gradlew buildPlugin exits 0. README.md contains "Mako Template Support" between the markers. CHANGELOG.md has ## [0.0.1] section. META-INF/pluginIcon.svg exists and is valid SVG.</done>
</task>

<task type="auto">
  <name>Task 2: Run verifyPlugin and confirm binary compatibility</name>
  <files>
    <!-- Read-only verification task: no files are created or modified by this task -->
  </files>
  <action>
Run the Plugin Verifier to confirm the plugin has no binary compatibility issues against the configured target IDE builds:

```bash
./gradlew verifyPlugin
```

The task is already configured in build.gradle.kts:
```kotlin
pluginVerification {
    ides {
        recommended()
    }
}
```

`recommended()` selects IDE builds based on the configured `platformVersion = 2025.2.5`. The verifier downloads IDE bundles to `~/.pluginVerifier/ides/` on first run (may take several minutes on first execution if not cached).

Expected output: "Plugin verification: no problems found" or similar success message. The task exits 0.

If verifyPlugin reports errors:
- "No such class" or "Method not found" errors indicate deprecated API usage — check MakoAnnotator.kt for any removed API calls (e.g., ensure `holder.newAnnotation()` builder is used, not deprecated `holder.createErrorAnnotation()`).
- If `recommended()` downloads an unexpectedly broad set of IDE versions, the output will list which IDEs were checked. This is informational, not a failure.
- If verifyPlugin fails due to network issues (offline environment), note this in the SUMMARY and consider this task passed once network is available.

Do NOT run `runPluginVerifier` — that was the Gradle Plugin 1.x task name. This project uses IntelliJ Platform Gradle Plugin 2.x where the task is `verifyPlugin`.
  </action>
  <verify>./gradlew verifyPlugin 2>&1 | grep -E "(BUILD|error|warning|problem)" | head -20</verify>
  <done>./gradlew verifyPlugin exits 0 with no binary compatibility errors reported. Output confirms "no problems found" or equivalent success message for the verified IDE builds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify release-readiness content</name>
  <what-built>Release-readiness content: README description updated, CHANGELOG 0.0.1 entry added, pluginIcon.svg created, verifyPlugin confirmed passing.</what-built>
  <how-to-verify>
    1. Open README.md — confirm the text between `<!-- Plugin description -->` and `<!-- Plugin description end -->` describes Mako Template Support (not the scaffold placeholder "This Fancy IntelliJ Platform Plugin...")
    2. Open CHANGELOG.md — confirm `## [0.0.1]` section exists listing the 8 implemented features
    3. Check `src/main/resources/META-INF/pluginIcon.svg` exists and opens as a valid SVG (a teal square with "M")
    4. Confirm `./gradlew buildPlugin` was green (build output in Task 1)
    5. Confirm `./gradlew verifyPlugin` was green (output in Task 2)
  </how-to-verify>
  <resume-signal>Type "approved" if content looks correct, or describe any issues with the README/CHANGELOG/icon</resume-signal>
</task>

</tasks>

<verification>
./gradlew buildPlugin — exits 0
./gradlew verifyPlugin — exits 0, no compatibility errors
README.md plugin description section contains real Mako plugin description
CHANGELOG.md has ## [0.0.1] with feature list
META-INF/pluginIcon.svg exists
</verification>

<success_criteria>
- README.md: content between Plugin description markers is real (not "This Fancy IntelliJ Platform Plugin...")
- CHANGELOG.md: has ## [0.0.1] release section with features
- pluginIcon.svg: exists at src/main/resources/META-INF/pluginIcon.svg, valid SVG, approximately 40x40
- ./gradlew buildPlugin: exits 0
- ./gradlew verifyPlugin: exits 0 with no binary errors
- Human checkpoint approved
</success_criteria>

<output>
After completion, create `.planning/phases/08-error-annotations-and-release-readiness/08-03-SUMMARY.md`
</output>
