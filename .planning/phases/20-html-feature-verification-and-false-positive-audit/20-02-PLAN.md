---
phase: 20-html-feature-verification-and-false-positive-audit
plan: "02"
type: execute
wave: 2
depends_on:
  - 20-01
files_modified: []
autonomous: true
requirements:
  - CRCT-01
  - CRCT-02

must_haves:
  truths:
    - "./gradlew check exits 0 — all tests pass after Phase 20-01 code additions"
    - "No compilation errors across the plugin codebase"
    - "MakoFileViewProviderTest reports 6 passing tests (4 pre-existing + 2 new CRCT tests)"
  artifacts:
    - path: "src/main/kotlin/com/schtilig/mako/lang/highlighting/MakoEditorHighlighter.kt"
      provides: "Confirmed to compile cleanly as part of full build"
    - path: "src/main/kotlin/com/schtilig/mako/lang/annotation/MakoErrorFilter.kt"
      provides: "Confirmed to compile cleanly and tests pass"
  key_links:
    - from: "./gradlew check"
      to: "MakoFileViewProviderTest"
      via: "test runner executes testNoFalsePositiveHtmlErrorOnMakoExpression and testNoFalsePositiveHtmlErrorOnMakoControlLines"
      pattern: "MakoFileViewProviderTest"
---

<objective>
Run `./gradlew check` to confirm the full test suite passes after the Phase 20-01 code additions. This is the automated gate before human IDE verification.

Purpose: Prevent regressions before opening a running IDE. All three new files (MakoEditorHighlighter, MakoEditorHighlighterProvider, MakoErrorFilter) plus plugin.xml must compile and all tests — existing and new — must pass.
Output: Zero test failures confirmed; build green.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-html-feature-verification-and-false-positive-audit/20-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and verify zero failures</name>
  <files></files>
  <action>
Run the full test and verification suite:

```bash
./gradlew check
```

Expected outcome: BUILD SUCCESSFUL with 0 test failures.

If `./gradlew check` fails:

1. **Compilation error in MakoEditorHighlighter or MakoEditorHighlighterProvider:**
   - Check that all imports are present (LayeredLexerEditorHighlighter, LayerDescriptor, SyntaxHighlighterFactory, TemplateDataLanguageMappings, HTMLLanguage)
   - Verify the `?: MakoSyntaxHighlighter()` fallback in the super-constructor call compiles (MakoSyntaxHighlighter is in the same package — no import needed)

2. **Compilation error in MakoErrorFilter:**
   - Verify `TemplateLanguageErrorFilter` import path: `com.intellij.codeInsight.highlighting.TemplateLanguageErrorFilter`
   - Verify `TokenSet` import: `com.intellij.psi.tree.TokenSet`
   - Verify `MakoFileViewProvider` import: `com.schtilig.mako.lang.MakoFileViewProvider`

3. **Test failure in testNoFalsePositiveHtmlErrorOnMakoExpression or testNoFalsePositiveHtmlErrorOnMakoControlLines:**
   - If the failure is "Expected no false-positive HTML errors... got: [...]", MakoErrorFilter is not suppressing errors — verify the TokenSet and class argument in the TemplateLanguageErrorFilter constructor
   - If the failure is "Cannot access class MakoErrorFilter", the highlightErrorFilter EP is not loaded — verify plugin.xml registration

4. **Existing test failure (pre-Phase 20 regression):**
   - This must not happen — all changes are additive. If an existing test fails, investigate which new file caused it and isolate the change.

Fix any failures and re-run `./gradlew check` until BUILD SUCCESSFUL.
  </action>
  <verify>
`./gradlew check` output contains "BUILD SUCCESSFUL" and the line `MakoFileViewProviderTest > testNoFalsePositiveHtmlErrorOnMakoExpression PASSED` and `MakoFileViewProviderTest > testNoFalsePositiveHtmlErrorOnMakoControlLines PASSED`.
  </verify>
  <done>
`./gradlew check` exits 0. All tests pass. No compilation errors. MakoFileViewProviderTest shows 6 passing test methods.
  </done>
</task>

</tasks>

<verification>
`./gradlew check` exits with BUILD SUCCESSFUL. Test output shows MakoFileViewProviderTest with 6 passing tests. No test failures anywhere in the test suite.
</verification>

<success_criteria>
- `./gradlew check` BUILD SUCCESSFUL, exit code 0
- MakoFileViewProviderTest: 6 passing tests (4 pre-existing + testNoFalsePositiveHtmlErrorOnMakoExpression + testNoFalsePositiveHtmlErrorOnMakoControlLines)
- No new failures in MakoLexerTest, MakoParsingTest, MakoAnnotatorTest, MakoCompletionTest, MakoStructureViewTest, MakoFoldingTest, MakoInjectionRangeTest, MakoInjectionHostTest, MakoPsiMixinTest, MyPluginTest
</success_criteria>

<output>
After completion, create `.planning/phases/20-html-feature-verification-and-false-positive-audit/20-02-SUMMARY.md`
</output>
